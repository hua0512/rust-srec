[package]
name = "pipeline-common"
version = "0.1.0"
edition = "2024"
description = "Common traits and implementations for media processing pipelines"
license = "MIT"

[dependencies]
thiserror = "1.0"
tracing = "0.1"
futures = "0.3"
