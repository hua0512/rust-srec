# =============================================================================
# Rust-Srec Frontend Environment Variables
# =============================================================================
# Copy this file to .env and modify as needed.
# For development, you may also create .env.development for dev-specific values.

# -----------------------------------------------------------------------------
# API Configuration (Vite Build-time)
# -----------------------------------------------------------------------------
# Backend API base URL - used during development and build
# This is embedded into the client bundle at build time (VITE_ prefix)
VITE_API_BASE_URL=http://localhost:8080/api

# -----------------------------------------------------------------------------
# Server-Side Configuration (Runtime)
# -----------------------------------------------------------------------------
# Backend URL for SSR server requests (without /api suffix)
# Used by the frontend server to proxy API requests
BACKEND_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Session Configuration (Required for Production)
# -----------------------------------------------------------------------------
# Secret key for session encryption - MUST be at least 32 characters
# Generate with: openssl rand -hex 32
SESSION_SECRET=your-session-secret-at-least-32-characters-long

# -----------------------------------------------------------------------------
# Cookie Security (Optional)
# -----------------------------------------------------------------------------
# Force secure cookies (HTTPS only)
# Values: 'true' = always secure, 'false' = never secure
# Default: auto-detect based on X-Forwarded-Proto header or NODE_ENV
# COOKIE_SECURE=true

# -----------------------------------------------------------------------------
# Timezone (Optional)
# -----------------------------------------------------------------------------
# Container timezone (used in Docker)
TZ=UTC
