# 引擎 (Engines)

下载器是应用程序的核心。它们负责从源头下载视频流。该应用程序支持三种下载器：`Mesio`、`FFMPEG` 和 `Streamlink`。每个下载器都有其独特的功能和局限性。

`Streamlink` 出现在两个相互独立的维度上，二者的名称很容易混淆。作为**下载器**，它是真正写出录像的进程，通过 `download_engine` 选择，也就是本页所介绍的内容；作为**提取器**，它负责在任何下载器运行之前解析出流地址，通过 `extractor` 选择，参见[引擎与提取器选择](./configuration.md#引擎与提取器选择)。两者可以各自单独设置。

> [!TIP]
> 对于 **Mesio** 用户，我们 **强烈建议** 启用 **FLV 一致性修复** 和 **HLS 一致性修复**。这两条流水线会修正或隔离时间戳与流结构变化，降低一次异常转换导致后续录制无法解码的概率。源站从未交付的媒体数据无法恢复。

`FFMPEG` 下载器是以兼容性为重点的外部引擎。它使用 C 语言编写，可下载 FLV 和 HLS 流，包括 FLV/RTMP 容器中的非标准 HEVC，但不支持多线程 HLS 下载。全新安装默认选择内置 Mesio 引擎实例；只有需要其封装或编解码兼容性时才应显式选择 FFmpeg。

> [!NOTE]
> 我们 Docker 镜像中提供的 FFMPEG 版本是来自 [yt-dlp/FFmpeg-Builds](https://github.com/yt-dlp/FFmpeg-Builds/) 的专用构建版本。该版本针对流媒体进行了优化，包含（或已合并入上游）了许多确保与 `yt-dlp` 顺畅集成的关键补丁，例如修复 AAC HLS 流截断问题、支持 Windows 长路径以及解码 FLV 容器中的非标准 HEVC。

### 录制文件名与状态跟踪

录制模板会展开配置中的日期标记，而主播名称和标题中的百分号保持字面含义。
例如，标题 `Top 5%d` 仍为 `Top 5%d`，不会替换成当月日期。启动时的输出根探测
使用相同的元数据字面值规则。

FFmpeg 录制与 Streamlink 重封装需要 info 级别日志和统计信息来跟踪分片及进度。
后端在最后提供 `-loglevel info -stats` 参数，因此自定义静默日志参数和 `-nostats`
不会关闭这些必要信息。

### 可执行文件检查

FFmpeg 和 Streamlink 的版本检查限时三秒，必要时随后执行有时间上限的进程清理。引擎测试和自定义引擎解析会异步执行检查。无法启动或未在期限内完成版本探测的可执行文件会报告为不可用。同步启动初始化也会等待同样的有界检查。

不存在的命名引擎配置保留默认引擎回退行为。数据库访问失败、已保存配置损坏及无效覆盖会导致解析失败，不再静默改变实际使用的配置。

### Streamlink 的 FFmpeg 可执行文件

Streamlink 下载引擎的后端引擎配置 JSON 支持可选字段 `ffmpeg_path`。
`binary_path` 指定 Streamlink 本身；`ffmpeg_path` 指定负责重封装其输出的
独立 FFmpeg 进程：

```json
{
  "binary_path": "streamlink",
  "ffmpeg_path": "/opt/media-tools/ffmpeg",
  "quality": "best"
}
```

后端按顺序选择配置中的 `ffmpeg_path`、环境变量 `FFMPEG_PATH`，最后使用
`PATH` 中的 `ffmpeg`。省略该字段或设为 `null` 会保留环境变量和默认值的行为。
包含空格的路径会作为单个可执行文件路径传递，不需要 shell 引号，也不能附带参数。
选择在创建引擎实例时确定；修改环境变量后需要重启后端。显式路径不会继承任何
已注册 FFmpeg 引擎的设置；可执行文件不存在时录制启动失败，不会自动回退。
空字符串或仅包含空白的字符串仍视为显式可执行文件值，不会触发回退；
如需清除覆盖，请省略该字段或设为 `null`。

引擎编辑器提供 **FFmpeg 路径** 字段，修改其他设置时会保留该路径。清空字段或选择
**使用环境默认值** 会恢复环境变量及默认可执行文件的查找方式。请输入后端服务器上的
路径，不要添加 shell 引号；路径文本会原样保留。

在模板的 Streamlink 覆盖设置中，清空字段或选择 **使用引擎设置** 会移除覆盖并继承
引擎路径。选择 **使用环境默认值** 则写入显式 `null`，为该模板清除引擎路径，改用
`FFMPEG_PATH` 或 `ffmpeg`。

### 停止 Streamlink 录制

对于通过源码兼容性校验的 Streamlink 8.5.0 安装，停止录制会通过专用的认证控制连接
请求停止继续采集。仍使用配置的 CLI、平台插件、认证信息、代理、画质和额外参数。
后端先执行有时间上限的插件加载能力探测，再加入内置控制组件；同一可执行文件的
并发探测会合并，结果会缓存。每次录制仍会重新验证控制组件及实际的读取器结构。

| 读取器配置 | 协作排空行为 |
| --- | --- |
| 分段 HLS/DASH，包括过滤和嵌套复用器 | 停止新的采集，完成已接纳的分片和初始化数据，再将读取缓冲及所有复用器输入读到 EOF。 |
| CPython 3.11/3.14、requests 2.34.2、urllib3 2.7.0 的 HTTP | 排空 HTTP 预读、TLS 已解密待取数据及 identity/gzip/deflate 解码缓冲；不包含不透明传输适配器或其他解码器。 |
| websocket-client 1.9.2 的 TwitCasting websocket | 先完成已接纳的消息接收及交付，再停止接收下一条消息。 |
| 文件路径和内存 BytesIO 输入 | 保留当前无缓冲文件读取，或完整排空已采集到内存的字节；不包含任意带预读缓冲的文件对象。 |

控制组件校验上游源码兼容性，而非仅信任版本文本。不支持的旧版、便携版或自定义
可执行文件仍可按原方式录制。不支持的读取器结构也保留原 CLI 行为，但停止时会报告
`Streamlink cooperative drain incomplete`，不会声称已验证缓冲排空。不会通过禁用
录制引擎功能来获得兼容性。

停止采集时，标准输出转发及标准错误处理会继续运行。成功的协作停止必须确认源输出
实际到达 EOF、管道转发完整且外部 FFmpeg 成功收尾；仅收到控制确认并不足够。
隐藏的 Windows 进程使用控制连接，不依赖控制台信号。内部 FFmpeg 复用进程及版本
验证进程保持隐藏；Windows 复用管道在断开前会刷新已接纳的字节。

这些进程、转发和重封装阶段使用本次录制剩余的停止期限，并为 FFmpeg 收尾预留时间。
后续应用关闭请求可缩短已经在停止的录制期限。必要的最终事件交付仍由原任务持有，
直到消费者接收：直接集成引擎的调用方必须持续读取事件通道；停滞消费者由工作进程
整体强制退出上限约束。超过期限仍会强制终止受控进程树，并可能截断尾部。保证范围
是已采集的数据和已接纳的工作，不包括尚未发现的未来分片、上游损坏或超出期限的工作。

两个子进程树继续受到约束，包括直接父进程退出后留下的后代。在 macOS 上，主进程
退出竞争仅使用剩余的进程树清理预算，不增加新的宽限期。该约束假定后代不会主动
逃离进程组或 Windows Job Object。

## 1. 引擎功能列表

|         功能          |                Mesio                 |                 FFMPEG                  |               STREAMLINK                |
| :----------------------: | :-----------------------------------: | :-------------------------------------: | :-------------------------------------: |
|       FLV 下载       |                  ✅                   |                   ✅                    |                   ❌                    |
|       HLS 下载       |        ✅ <br/>(多线程)        |                   ✅                    |         ✅ <br/>(多线程)         |
| 录制时长统计 | ✅ <br/>(不支持原始数据模式) |                   ✅                    |                   ✅                    |
|  下载码率统计  |                  ✅                   | ✅ <br/>(不支持 -f segmentation) | ✅ <br/>(不支持 -f segmentation) |
|    按大小分段     | ✅ <br/>(不支持原始数据模式) | ✅ <br/>(不支持 -f segmentation) |                   ✅                    |
|  按时长分段   | ✅ <br/>(不支持原始数据模式) |                   ✅                    |                   ✅                    |
|     下载格式      |           FLV, M3U8, TS, M4S          |         支持其他格式          |         支持其他格式          |
|    FLV 一致性修复      |             ✅ (推荐)                 |                   ❌                    |                   ❌                    |
|    HLS 一致性修复      |             ✅ (推荐)                 |                   ❌                    |                   ❌                    |
|        CPU 使用率         |                  最低                 |                   低                   |                   低                   |
|       内存使用率       |                  最低                 |                   低                   |                 中                  |

## 2. FLV 一致性修复

启用后，FLV 数据会在进入写入器前通过一条有序修复链。该链保留媒体载荷，主要修复容器层结构：

| 问题 | 流水线行为 |
| --- | --- |
| 缺少 FLV 文件头 | 在首个标签前插入有效文件头 |
| 视频或音频格式变化 | 开始写入新文件，并补充播放所需的文件头 |
| 视频数据乱序 | 在保持内存有界的前提下重排相邻标签 |
| 时间戳跳变或回退 | 调整时间戳以保持播放连续性 |
| 重复媒体标签或序列头 | 启用对应选项时过滤 |
| 达到文件大小或时长限制 | 视频在关键帧边界轮换，仅音频输出则在下一个标签轮换 |
| FLV 统计与快速定位索引 | 在文件关闭前更新元数据 |

元数据更新不会重写已经完成的媒体数据。如果可用元数据空间不足，会缩短可选的快速定位索引。带过滤或加密标记的脚本载荷保持不变。

## 3. 原始数据模式 (Raw Data Mode)

原始数据模式是 **Mesio** 引擎支持的一种高性能下载模式。在该模式下，引擎会将从网络接收到的流数据直接写入磁盘，而不对内部媒体包（头信息、帧、元数据）进行解析或处理。

### 主要特点：
- **极致性能**：由于不进行包解析或重采样（Re-muxing），CPU 和内存占用被降至绝对最低。
- **零开销**：非常适合高带宽流或资源受限的环境（如入门级 NAS 或 VPS）。仅在网络稳定且 CDN / 流来源没有数据波动（如媒体头部信息变更）时推荐使用。

### 局限性：
由于不检查包结构，启用原始数据模式时，以下高级功能将无法使用：
- **数据统计**：无法实时计算录制时长和下载码率统计。
- **自动分段**：引擎无法检测帧边沿或时长，因此无法按大小或时长进行精确分段。
- **修复功能**：无法应用 FLV 一致性修复或 HLS 一致性修复，因为这些功能需要包级别的操作。

## 4. HLS 一致性修复 (Mesio 独占)

Mesio 的 HLS 下载反应堆与 HLS 修复流水线职责不同：

- **下载引擎**负责对已抓取分段排序，先写入 fMP4 媒体所需的初始化数据，应用已配置的缺口策略，并保留播放列表不连续所形成的输出边界。
- **一致性修复**会在已交付分段出现不兼容的编解码器、分辨率、节目布局或 fMP4 初始化数据变化时开始写入新文件，并执行文件大小与时长限制。

该流水线不会改写 TS 或 fMP4 载荷内的时间戳，不能重建缺失媒体，也不会转码。被跳过的分段仍然是可观测的缺口；流水线会保持已交付输出的顺序，并在检测到需要新文件的格式变化时轮换输出。

## 5. Mesio 架构

Mesio 是**进程内的 Rust 引擎**，采用基于反应堆的 HLS 下载器，并由 HLS 与 FLV 共用统一的下载会话模型。架构图与内部工作原理详解请参阅专门的 [Mesio 引擎](./mesio.md) 页面。
