# ============================================================
# Rust-Srec 环境变量配置
# ============================================================
# 将此文件复制为 .env 并配置相关数值

# ============================================================
# 时区配置
# ============================================================
TZ=Asia/Shanghai

# ============================================================
# Docker 镜像版本
# ============================================================
VERSION=latest

# ============================================================
# 目录路径 (主机挂载卷)
# ============================================================
DATA_DIR=./data
CONFIG_DIR=./config
OUTPUT_DIR=./output
LOG_DIR=./logs

# ============================================================
# 端口配置
# ============================================================
API_PORT=12555
FRONTEND_PORT=15275

# ============================================================
# 后端配置
# ============================================================

# 日志级别: trace, debug, info, warn, error
RUST_LOG=info

# 数据库连接字符串
DATABASE_URL=sqlite:///app/data/rust-srec.db

# 后端严格关闭期限（秒）。最后 2 秒预留给强制终止隔离运行时，
# 以防优雅关闭停滞。
RUST_SREC_SHUTDOWN_TIMEOUT_SECS=30
RUST_SREC_SHUTDOWN_FORCE_RESERVE_SECS=2
# Docker Compose 的等待时间必须长于后端期限，之后才能发送 SIGKILL。
RUST_SREC_CONTAINER_STOP_GRACE_PERIOD=35s

# 可选覆盖。默认标记文件存放在 SQLite 数据库旁边。
# RUST_SREC_RUNTIME_MARKER_PATH=/app/data/rust-srec.db.runtime-generation.dirty

# ============================================================
# 输出根写入门（#508）
# ============================================================
# 以逗号分隔的绝对路径列表，作为写入门的输出根边界。
# 未设置时，写入门会自动从所有配置层级（全局、平台、模板、单主播）
# 中发现挂载根。仅在以下情况需要显式设置：
#  - 想要探测尚未被任何主播配置引用的挂载（如计划迁移的新卷）
#  - 想要将 /rec/{platform}/... 布局合并到单一 /rec 门键下
# 示例: RUST_SREC_OUTPUT_ROOTS=/rec,/mnt/backup
# RUST_SREC_OUTPUT_ROOTS=

# ============================================================
# 后端语言
# ============================================================
# 后端发出的通知字符串的语言环境。影响所有通知事件
# （直播上/下线、录制生命周期、分段、流水线任务、系统告警、凭据事件），
# 包括推送到 Telegram、Gotify、Discord、Webhook、邮件和 Web Push 的通知。
# 支持：en、zh-CN。默认 en。
# RUST_SREC_LOCALE=zh-CN

# ============================================================
# JWT 认证 (必需)
# ============================================================

# 用于签名 JWT 令牌的密钥 (必需 - 请生成一个安全的随机字符串)
JWT_SECRET=

# JWT 签发者和受众标识符
JWT_ISSUER=rust-srec
JWT_AUDIENCE=rust-srec-api

# ============================================================
# 令牌过期时间
# ============================================================

# 访问令牌有效期（秒，默认：1小时）
ACCESS_TOKEN_EXPIRATION_SECS=3600

# 刷新令牌有效期（秒，默认：7天）
REFRESH_TOKEN_EXPIRATION_SECS=604800

# ============================================================
# 密码策略
# ============================================================

# 最小密码长度
MIN_PASSWORD_LENGTH=8

# ============================================================
# 资源限制 (Docker)
# ============================================================
# CPU_LIMIT=4
# MEMORY_LIMIT=4G
# CPU_RESERVATION=1
# MEMORY_RESERVATION=512M

# ============================================================
# 自动更新 (Watchtower, 可选)
# ============================================================
# 仅在启用 `autoupdate` profile 时生效：
#   docker compose --profile autoupdate up -d
# 需要使用可变镜像标签（VERSION=latest 或 dev）；固定的 vX.Y.Z 标签
# 不会收到更新。仅在系统空闲（无录制、无执行中的流水线任务）时
# 才会重建容器，参见 GET /api/health/idle。
#
# 检查更新的间隔（秒，默认 3600 = 1 小时）。
# WATCHTOWER_POLL_INTERVAL=3600

# ============================================================
# 前端配置
# ============================================================

# 后端 API 地址 (Docker 内部网络地址)
BACKEND_URL=http://rust-srec:8080

# 会话 Cookie 加密密钥 (必需 - 至少 32 个字符)
SESSION_SECRET=

# Cookie 安全标志: 'true' = 始终启用, 'false' = 禁用, 留空 = 自动检测
# 自动检测使用 X-Forwarded-Proto 头 (推荐在反向代理后使用)
COOKIE_SECURE=

# ============================================================
# 浏览器通知 (Web Push / VAPID) (可选)
# ============================================================
# 生成 VAPID 密钥 (base64url, 无 padding)，例如：
#   docker run --rm ghcr.io/hua0512/rust-srec:${VERSION:-latest} /app/rust-srec-vapid
#   # 或: npx --yes web-push generate-vapid-keys
#
# 注意：浏览器推送通知需要 HTTPS（或 localhost）。
WEB_PUSH_VAPID_PUBLIC_KEY=
WEB_PUSH_VAPID_PRIVATE_KEY=
WEB_PUSH_VAPID_SUBJECT=mailto:admin@localhost
